import { Oxygen, Quattrocento } from "next/font/google";

export const oxygen = Oxygen({
  weight: ["400", "700"],
  subsets: ["latin"],
  display: "swap",
});

export const quattrocento = Quattrocento({
  weight: ["400", "700"],
  subsets: ["latin"],
  display: "swap",
});
